apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-blog
  namespace: blog
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi